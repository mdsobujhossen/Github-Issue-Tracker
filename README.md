
    - 1️⃣ What is the difference between var, let, and const?
    Answer : 
    a. var is global scop variable we can redeclear and reassign the value (we dont use it in real life it is old variable)

    b. let is functional scope variable we can reassign the value but we can't redeclear the variable we use it when we chenge or update the value later. 

    c.const is can't redclear and can't reassign the value we use it when we want fixed value.

    - 2️⃣ What is the spread operator (...)?
    answer: spread oparetor is spread the element of an array, object, function arguments.
    
    - 3️⃣ What is the difference between map(), filter(), and forEach()?
    Answer :
    a. map method return new array. we used it when we want change to each element of an array
    b. filter method also return a new array with the elements that match condition.
    c.foreach method don't return new array it go through each element of an array and execution the code writen for this method
    - 4️⃣ What is an arrow function?
      Answer: an arrow function is shorter version of function it's 
    - 5️⃣ What are template literals?
    Answer: Template litaral is very powerfull method to write string. we can inseart variable we can call fucntion and can use ternery oparator.


